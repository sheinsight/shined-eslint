export function main() {
  new Promise(async (resolve, reject) => {
    var a = `s`
    if (true) {
      debugger
    } else {
    }
    throw a
  })
}
var c = 1

a = undefined

const reg = /a|a/

const b = a == undefined
