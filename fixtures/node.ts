import fs from 'fs'
import { num } from './ts.js'

export function main() {
  new Promise(async (resolve, reject) => {
    var a = `s`
    if (true) {
      debugger
    } else {
    }
    throw a
  })
}
console.log('num', fs)

process.exit()

const reg = /a|a/

async function test() {}

test().then(() => {})
